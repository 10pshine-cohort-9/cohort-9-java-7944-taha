spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/demo_jpa
    username: postgres
    password: Malikpc2627.

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true